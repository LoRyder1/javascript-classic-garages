<header>
  <nav>
    <% if logged_in? %>
      <%= "Hi " + current_user.username  %>
      <%= link_to "Home", garages_path %>
      <%= link_to "Logout", logout_path, method: :delete %>
    <% else %>
      <%= link_to "Login", new_session_path %>
      <%= link_to "Sign Up", new_user_path %>
    <% end %>
  </nav>
</header>